pipeline {
    agent any 
    stages {
        stage('--clone--') { 
            steps {
                sh "rm -rf Node-Docker-Jenkins"
                sh "git clone https://github.com/MosePrime13/Node-Docker-Jenkins.git"
            }
        }
        stage('--test--') { 
            steps {
                // 
            }
        }
        stage('--deploy--') { 
            steps {
                // 
            }
        }
    }
}